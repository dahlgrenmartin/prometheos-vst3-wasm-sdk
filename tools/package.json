{
  "name": "@prometheos/webvst-tools",
  "private": true,
  "type": "module",
  "bin": {
    "webvst": "dist/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:conformance": "vitest run ../tests/conformance",
    "typecheck": "tsc --noEmit",
    "build": "tsc --outDir dist --noEmit false",
    "webvst": "pnpm run build && node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
